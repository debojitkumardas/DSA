# ignore all .o files
*.o

# ignore all .o files
*.d

# ignore all files in any directory named .cache
.cache/

# ignore all files in bin dir
bin/

#ignore compile_commands.json files
compile_commands.json

# ignore all files in the docs dir
docs/

# but do track lib.out, even though you're ignoring .out files above
# !lib.o

# only ignore the TODO file in the current directory, not subdir/TODO
# /TODO

# ignore all files in any directory named build
# build/

# ignore doc/notes.txt, but not doc/server/arch.txt
# doc/*.txt

# ignore all .pdf files in the doc/ directory and any of its subdirectories
# doc/**/*.pdf
